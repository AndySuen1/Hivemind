node_modules/
dist/
*.log
.env
.env.local
.env.*.local

# build / IDE
.turbo/
.next/
.vscode/
.idea/
# TypeScript 增量构建缓存（每次 typecheck 都会变，纯构建产物，勿提交）
*.tsbuildinfo

# Claude Code 本地配置（含本机绝对路径与一次性权限规则，勿提交）
.claude/

# data（含 SQLite 主库与 WAL/shm/journal 旁文件）
data/*.db
data/*.db-*
data/logs/

# 配置导出文件（可能含明文密钥，切勿提交）
hivemind-config-*.json
discord-agent-hub-config-*.json

# OS
.DS_Store
Thumbs.db
